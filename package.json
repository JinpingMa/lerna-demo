{
  "name": "lerna-demo",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "test": "lerna run test --scope={common,server}",
    "new-version": "lerna version --conventional-commits",
    "diff": "lerna diff"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
